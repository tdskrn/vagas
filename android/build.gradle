buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.2' // Verifique a versão correta para o seu projeto
        classpath 'com.google.gms:google-services:4.3.15' // Plugin do Firebase
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
